when:
  event: [push, pull_request, manual]

steps:
  critical:
    image: alpine:3.20
    commands:
      - apk add --no-cache lua5.4
      - find . -path ./.git -prune -o -name '*.lua' -print0 | xargs -0 -r luac5.4 -p
